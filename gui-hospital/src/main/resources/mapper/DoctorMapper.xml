<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gui.hospital.mapper.DoctorMapper">
<!--
 List<Doctor> findByHdId(@Param("hdId") String hdId, @Param("dayTime") Date dayTime);
 -->
    <select id="findByHdId" resultType="com.gui.hospital.model.Doctor">
        SELECT * FROM `tb_doctor` WHERE id IN (
            SELECT doctor_id  FROM `tb_scheduling`
                              WHERE day_time = #{dayTime}
                                AND hd_id = #{hdId}
                                AND on_line_count > 0
        )
    </select>

</mapper>